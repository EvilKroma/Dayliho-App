import 'package:flutter/material.dart';

class Reservations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Réservations'),
      ),
      body: Center(
        child: Text('Page des réservations'),
      ),
    );
  }
}
